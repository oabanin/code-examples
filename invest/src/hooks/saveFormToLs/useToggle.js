import { useCallback, useState } from 'react';const useToggle = (initialState = false) => {  const [state, setState] = useState(initialState);  const [toggleData, setToggleData] = useState(null);  const toggle = useCallback(    (data) =>      new Promise((resolve) => {        if (data) {          setToggleData(data);          resolve(data);        } else {          // delay data clear to avoid text blinking in modal window          setTimeout(() => {            setToggleData(null);            resolve(true);          }, 200);        }        setState((state) => !state);      }),    [],  );  return [state, toggle, toggleData];};export { useToggle };